type Blog @model {
  id: ID!
  name: String!
  posts: [Post]! @connection(name: "BlogsPosts")
}

type Post @model {
  id: ID!
  title: String!
  content: String
  belongsTo: Blog! @connection(name: "BlogsPosts")
}